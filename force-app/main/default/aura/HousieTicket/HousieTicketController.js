({
    init: function(component, event, helper) {
        //helper.getTicketNumbers(component);
    },
    generateTicket: function(component, event, helper) {
        helper.generateTicketNumbers(component);
    }
})